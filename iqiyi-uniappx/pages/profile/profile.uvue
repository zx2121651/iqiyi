<template>
  <scroll-view class="container" :scroll-y="true">
    <!-- 顶部透明状态栏和操作区 -->
    <view class="header-actions">
      <view class="action-left">
        <text class="action-icon">⎚</text>
      </view>
      <view class="action-right">
        <text class="action-icon">🔔</text>
        <text class="action-icon">⚙️</text>
      </view>
    </view>

    <view class="content-wrapper">
      <!-- 用户头部信息区 -->
      <view class="user-info-section">
        <image class="avatar" src="https://dummyimage.com/100x100/333/fff&text=Guest" mode="aspectFill"></image>
        <view class="user-text-box">
          <text class="user-name">{{ isLogin ? 'VIP用户' : '点击登录/注册' }}</text>
          <text class="user-id">{{ isLogin ? '爱奇艺号: 12345678' : '登录后享受更多精彩内容' }}</text>
        </view>
        <text class="arrow">></text>
      </view>

      <!-- VIP 黑金卡片 -->
      <view class="vip-card">
        <view class="vip-card-header">
          <text class="vip-title">VIP 黄金会员</text>
          <button class="vip-btn">立即开通</button>
        </view>
        <text class="vip-desc">开通即享万部大片免费看、免广告特权</text>
      </view>

      <!-- 核心服务金刚区 (横向排列) -->
      <view class="service-section">
        <text class="section-title">我的服务</text>
        <view class="service-grid">
          <view class="service-item" v-for="(item, index) in services" :key="index">
            <view class="service-icon-box">
              <text class="service-icon" :style="{ color: item.color }">{{ item.icon }}</text>
            </view>
            <text class="service-name">{{ item.name }}</text>
          </view>
        </view>
      </view>

      <!-- 纵向列表菜单区 -->
      <view class="menu-list">
        <view class="menu-item" v-for="(item, index) in tools" :key="index" :class="{ 'no-border': index === tools.length - 1 }">
          <view class="menu-left">
            <text class="menu-icon">{{ item.icon }}</text>
            <text class="menu-name">{{ item.name }}</text>
          </view>
          <view class="menu-right">
            <text v-if="item.desc !== ''" class="menu-desc">{{ item.desc }}</text>
            <text class="arrow">></text>
          </view>
        </view>
      </view>
    </view>
  </scroll-view>
</template>

<script setup lang="uts">
import { ref } from 'vue'

const isLogin = ref(false)

const services = [
  { icon: '⏱️', color: '#fb923c', name: '观看历史' },
  { icon: '⭐', color: '#facc15', name: '我的收藏' },
  { icon: '⬇️', color: '#60a5fa', name: '离线缓存' },
  { icon: '▶️', color: '#4ade80', name: '稍后再看' },
]

const tools = [
  { icon: '👛', name: '我的钱包', desc: '领红包' },
  { icon: '🎧', name: '联系客服', desc: '' },
  { icon: '⚙️', name: '设置', desc: '' },
]
</script>

<style>
.container {
  display: flex;
  flex: 1;
  flex-direction: column;
  background-color: #121212;
  height: 100%;
}
.header-actions {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  padding: 40px 16px 16px 16px;
  background-color: transparent;
  position: sticky;
  top: 0;
  z-index: 50;
}
.action-left {
  display: flex;
}
.action-right {
  display: flex;
  flex-direction: row;
  gap: 16px;
}
.action-icon {
  font-size: 24px;
  color: #ffffff;
  margin-left: 16px;
}
.content-wrapper {
  padding: 0 16px 80px 16px;
}

/* 用户头部信息区 */
.user-info-section {
  display: flex;
  flex-direction: row;
  align-items: center;
  margin-bottom: 24px;
  margin-top: 8px;
}
.avatar {
  width: 64px;
  height: 64px;
  border-radius: 32px;
  background-color: #333333;
  border-width: 2px;
  border-color: rgba(255,255,255,0.1);
  border-style: solid;
}
.user-text-box {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  margin-left: 16px;
}
.user-name {
  font-size: 20px;
  font-weight: bold;
  color: #ffffff;
}
.user-id {
  font-size: 12px;
  color: #888888;
  margin-top: 4px;
}
.arrow {
  color: #888888;
  font-size: 16px;
}

/* VIP 黑金卡片 */
.vip-card {
  width: 100%;
  background-image: linear-gradient(to right, #e8c081, #b3853b);
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 24px;
}
.vip-card-header {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}
.vip-title {
  color: #3a280c;
  font-size: 18px;
  font-weight: bold;
  font-style: italic;
}
.vip-btn {
  background-color: #3a280c;
  color: #e8c081;
  font-size: 12px;
  font-weight: bold;
  border-radius: 16px;
  padding: 4px 12px;
  margin: 0;
  line-height: 1.5;
}
.vip-desc {
  color: #593d14;
  font-size: 12px;
  font-weight: bold;
}

/* 核心服务金刚区 */
.service-section {
  background-color: #1f1f1f;
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 24px;
}
.section-title {
  font-size: 14px;
  font-weight: bold;
  color: rgba(255,255,255,0.9);
  margin-bottom: 16px;
}
.service-grid {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
}
.service-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.service-icon-box {
  background-color: rgba(255,255,255,0.05);
  padding: 12px;
  border-radius: 16px;
  margin-bottom: 8px;
}
.service-icon {
  font-size: 28px;
}
.service-name {
  font-size: 12px;
  color: #888888;
  font-weight: bold;
}

/* 纵向列表菜单区 */
.menu-list {
  background-color: #1f1f1f;
  border-radius: 12px;
  overflow: hidden;
}
.menu-item {
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  border-bottom-width: 1px;
  border-bottom-color: rgba(255,255,255,0.05);
  border-bottom-style: solid;
}
.no-border {
  border-bottom-width: 0;
}
.menu-left {
  display: flex;
  flex-direction: row;
  align-items: center;
}
.menu-icon {
  font-size: 20px;
  color: #888888;
  margin-right: 12px;
}
.menu-name {
  font-size: 14px;
  font-weight: bold;
  color: rgba(255,255,255,0.9);
}
.menu-right {
  display: flex;
  flex-direction: row;
  align-items: center;
}
.menu-desc {
  font-size: 12px;
  color: #888888;
  margin-right: 8px;
}
</style>
